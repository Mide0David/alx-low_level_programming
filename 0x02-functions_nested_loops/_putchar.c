#include<unistd.h>
#include <stdio.h>

/**
 * _putchar - to output the word _putchar
 * @c: the char to output
 * Return: it returns zero if successful
 */
int _putchar(char c)
{
	(write(1, &c, 1);
	 return c;
}
