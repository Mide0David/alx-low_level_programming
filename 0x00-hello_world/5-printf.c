#include<stdio.h>
/**
 * main - the start point it begin outputting the words in the printf
 * Return: returns 0 if programs runs successfully
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
