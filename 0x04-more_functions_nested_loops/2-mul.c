#include <stdio.h>

/**
 * mul - multiplies two numbers
 * @a: receives the first number argument
 * @b: receives the second number argument
 * Return: returns the result of the multiplication
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
